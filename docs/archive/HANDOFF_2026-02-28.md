# VideoForge Handoff — 2026-02-28

Date: 2026-02-28
Branch: main (working tree)

## Session Summary

This session focused on preventing system lockups/black-screen events when native processing is selected.

### 1) Native feature no longer defaults on
- Updated `src-tauri/Cargo.toml`:
  - `default = []`
  - `native_engine` remains opt-in.

### 2) Runtime native gate added
- Added explicit runtime guard using env var:
  - `VIDEOFORGE_ENABLE_NATIVE_ENGINE=1` required to run native.
- Implemented in:
  - `src-tauri/src/commands/native_engine.rs`
  - `src-tauri/src/commands/rave.rs` (`rave_upscale`, `rave_benchmark`)

### 3) UI native toggle path clarified and guarded
- Confirmed header native toggle routes through `rave_upscale` (RAVE CLI path), not direct `upscale_request_native`.
- Added UI error-hint mapping for `native_engine_disabled`:
  - `ui/src/hooks/useRaveIntegration.ts`

### 4) Native path consolidation
- `upscale_request_native` converted to compatibility shim delegating to `rave_upscale`.
- Result: one effective native execution route for both header toggle and direct native command calls.

## Validation Performed

- `cargo check --manifest-path src-tauri/Cargo.toml`
- `cargo check --manifest-path src-tauri/Cargo.toml --features native_engine`

Both checks passed in this session.

## Residual Risks / Notes

- `src-tauri/src/commands/native_engine.rs` still contains legacy direct-pipeline implementation code that is now unused by `upscale_request_native` (dead-code warnings when `native_engine` is enabled).
- RAVE path depends on external runtime/provider state (CUDA/ORT/TensorRT) and remains environment-sensitive.

## Recommended First Step Next Session

1. Remove legacy unused direct native pipeline code from `src-tauri/src/commands/native_engine.rs` to reduce ambiguity and warning noise.
2. Run native smoke/perf checks after cleanup:
   - `tools/ci/check_native_smoke_perf.ps1`

## Files Touched This Session

- `src-tauri/Cargo.toml`
- `src-tauri/src/commands/native_engine.rs`
- `src-tauri/src/commands/rave.rs`
- `src-tauri/src/bin/smoke.rs`
- `ui/src/hooks/useRaveIntegration.ts`
